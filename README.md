# Система бронирования переговорок

Telegram бот для управления бронированием переговорок в офисе. Проект создан в рамках экзамена курса Vibe Coding 2025 (ITMO, магистратура ПИ).

## Проблема

В офисе есть несколько переговорок, которые сотрудники используют для встреч. Часто возникают ситуации:
- Сотрудник приходит к переговорке, а она занята
- Встреча закончилась раньше, но переговорка остается забронированной
- Непонятно, кто занял переговорку и когда она освободится

## Решение

Telegram бот с простыми командами для:
- Просмотра списка всех переговорок
- Проверки доступности переговорок
- Бронирования переговорки на определенное время
- Досрочного освобождения переговорки
- Проверки статуса конкретной переговорки
- Просмотра своих бронирований

Все данные хранятся в SQLite базе данных с защитой от конфликтов бронирований.

## Архитектура

Проект следует принципам Clean Architecture:

```
src/
├── database.py       # Низкоуровневая работа с SQLite
├── repository.py     # Repository pattern (SQLite + InMemory для тестов)
├── service.py        # Бизнес-логика (валидация, проверки, форматирование)
├── bot.py           # Telegram интерфейс (тонкая обертка над service)
└── models.py        # Доменные модели (deprecated, используется service)
```

**Преимущества архитектуры:**
- Service layer независим от Telegram - можно легко добавить CLI или Web API
- InMemory repository для быстрых тестов (без SQLite, без I/O)
- Четкое разделение ответственности (data access / business logic / presentation)

## Установка

### 1. Клонирование репозитория

```bash
git clone <repo-url>
cd vibe-coding-course-final
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуется Python 3.8+

### 3. Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username для бота)
4. Скопируйте полученный token

### 4. Настройка переменных окружения

Создайте файл `.env`:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

Или экспортируйте переменную:

```bash
export TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 5. Инициализация данных (опционально)

Создайте тестовые переговорки:

```python
from src.database import Database
from src.repository import SQLiteRepository

repo = SQLiteRepository("bookings.db")
repo.add_room("Марс", 6)
repo.add_room("Венера", 4)
repo.add_room("Юпитер", 8)
repo.add_room("Сатурн", 10)
```

Или через Python:

```bash
python3 -c "
from src.repository import SQLiteRepository
r = SQLiteRepository()
r.add_room('Марс', 6)
r.add_room('Венера', 4)
r.add_room('Юпитер', 8)
r.add_room('Сатурн', 10)
"
```

## Использование

### Запуск бота

```bash
python3 -m src.bot
```

Или:

```bash
python3 src/bot.py
```

### Команды бота

#### `/start`
Показать справку по командам

#### `/rooms`
Показать список всех переговорок

```
📋 Все переговорки:

• Марс (вместимость: 6)
• Венера (вместимость: 4)
• Юпитер (вместимость: 8)
• Сатурн (вместимость: 10)
```

#### `/available`
Показать свободные и занятые переговорки на текущий момент

```
🟢 Свободные переговорки:

• Венера (вместимость: 4)
• Юпитер (вместимость: 8)

🔴 Занятые переговорки:

• Марс - занят до 15:00
• Сатурн - занят до 16:30
```

#### `/book <название> <время>`
Забронировать переговорку

```
/book Марс 15:00-16:00
✅ Марс забронирован на 15:00-16:00
```

Если переговорка занята:
```
/book Марс 15:00-16:00
❌ Марс занят с 14:00 до 16:00
```

#### `/release <название>`
Освободить переговорку досрочно (только свою бронь)

```
/release Марс
✅ Марс освобожден
```

Если пытаетесь освободить чужую бронь:
```
/release Марс
❌ Марс забронирован не вами
```

#### `/status <название>`
Проверить текущий статус переговорки

```
/status Марс
Марс: Иван Иванов, до 16:00
```

Или если свободна:
```
/status Марс
Марс свободен
```

#### `/mybooks`
Показать все ваши бронирования

```
📅 Ваши бронирования:

• Марс
  14.01.2026 15:00 - 16:00

• Венера
  14.01.2026 17:00 - 18:00
```

## Тестирование

Проект использует BDD (Behavior-Driven Development) с pytest-bdd.

### Запуск всех тестов

```bash
pytest tests/steps/test_room_steps.py -v
```

### Запуск с подробным выводом

```bash
pytest tests/steps/test_room_steps.py -v --gherkin-terminal-reporter
```

### Запуск конкретного сценария

```bash
pytest tests/steps/test_room_steps.py::test_успешное_бронирование_свободной_переговорки -v
```

### BDD сценарии

Все сценарии описаны на русском языке в `tests/features/`:

1. **list_rooms.feature** - Просмотр всех переговорок
2. **view_rooms.feature** - Просмотр свободных переговорок
3. **book_room.feature** - Бронирование переговорки
4. **release_room.feature** - Досрочное освобождение
5. **who_booked.feature** - Информация о бронировании

Пример сценария:

```gherkin
Scenario: Успешное бронирование свободной переговорки
  Given в системе есть переговорка "Марс"
  And пользователь "Вася" авторизован
  And "Марс" свободен
  When пользователь вызывает команду "/book Марс 15:00-16:00"
  Then бронирование создается на имя "Вася"
  And бот отвечает "✅ Марс забронирован на 15:00-16:00"
```

### Преимущества тестирования

- **Быстрые тесты**: InMemory repository (0.04s для 10 тестов)
- **Изолированность**: каждый тест получает свежий репозиторий
- **Читаемость**: тесты читаются как спецификация на русском языке
- **Покрытие**: все основные сценарии использования

## Структура проекта

```
vibe-coding-course-final/
├── src/
│   ├── __init__.py
│   ├── database.py          # SQLite операции (CREATE TABLE, queries)
│   ├── repository.py        # Repository pattern (InMemory + SQLite)
│   ├── service.py           # Бизнес-логика
│   ├── bot.py              # Telegram бот
│   └── models.py           # Доменные модели (legacy)
├── tests/
│   ├── conftest.py         # Pytest fixtures (test_context с InMemoryRepo)
│   ├── features/           # BDD сценарии (Gherkin)
│   │   ├── list_rooms.feature
│   │   ├── view_rooms.feature
│   │   ├── book_room.feature
│   │   ├── release_room.feature
│   │   └── who_booked.feature
│   └── steps/
│       └── test_room_steps.py  # Step definitions
├── exam-process/           # Документация процесса экзамена
│   ├── README.md
│   └── 01-project-selection-plan.md
├── README.md
├── CLAUDE.md              # Инструкции для Claude Code
├── requirements.txt
├── .gitignore
└── bookings.db           # SQLite база (создается автоматически)
```

## Технологии

- **Python 3.8+**
- **aiogram 3.3.0** - асинхронный Telegram Bot API framework
- **SQLite3** - встроенная СУБД для хранения данных
- **pytest 7.4.3** - фреймворк для тестирования
- **pytest-bdd 6.1.1** - BDD расширение для pytest

## Разработка

Проект создан с использованием методологии Vibe Coding:

1. **BDD-first**: сценарии написаны ДО реализации
2. **TDD цикл**: Red (падающие тесты) → Green (реализация) → Refactor (улучшение)
3. **AI pair programming**: Claude Code как co-author
4. **Clean commits**: каждый коммит = логически завершенный шаг

### История коммитов

```bash
git log --oneline
```

Пример вывода:
```
c19de0f Refactor to clean architecture with Repository and Service layers
f218d66 Implement Telegram bot with room booking commands
f31d924 Add business models for rooms and bookings
bd0e955 Implement SQLite database layer
ad2803e Add BDD tests with failing stubs (RED phase)
b34ec87 Add BDD scenarios with bot commands
e8c1d9f Initial project structure
69b88d3 Add exam process documentation
```

## Возможные улучшения

1. **Уведомления**: напоминания за 5 минут до окончания брони
2. **Периодические бронирования**: повторяющиеся встречи
3. **Поиск по времени**: найти свободную переговорку на конкретное время
4. **Вместимость**: фильтр по количеству участников
5. **Календарь**: интеграция с Google Calendar
6. **Аналитика**: статистика использования переговорок
7. **Админ-панель**: управление переговорками через бота
8. **Фото переговорок**: помочь сотрудникам найти нужную комнату

## Лицензия

MIT

## Автор

Создано для экзамена курса Vibe Coding 2025 (ITMO, магистратура ПИ)

Co-Authored with Claude Code
